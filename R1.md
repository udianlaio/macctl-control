# R1 GitHub 补充控制通道

R1 在 R0 只读控制基础上增加：

- `full.audit`：一次请求完成 Mac、Linux、备份、SentinelX、GitHub runner 的固定只读巡检。
- Issue 状态：`macctl:request` → `macctl:running` → `macctl:passed` / `macctl:failed`。
- 公开仓库只回传非敏感摘要；完整输出保留在 Hermes 本地 ledger。
- SentinelX 与 GitHub runner 均可通过 GitHub 通道独立做只读健康检查。

GitHub 仍是 SentinelX 的 durable / asynchronous 补充通道，不替代实时交互控制。
