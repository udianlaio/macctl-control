# R2 Operations

Autonomous typed operations currently exposed by `/usr/local/libexec/macctl-github-dispatch` are discoverable at runtime through `control.capabilities`. The GitHub workflow publishes only non-sensitive summaries and stores complete execution details in the Hermes local ledger.
